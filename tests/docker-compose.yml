version: '2'

services:
  nginx:
    build:
      context: .
      dockerfile: nginx/Dockerfile
    ports:
      - 80:80
    depends_on:
      - fpm

  fpm:
    image: jorge07/ubuntu-php:7.0-dev
    expose:
      - 9000
    volumes:
      - "$PWD:/app"